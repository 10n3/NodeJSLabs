module.exports = (arr) =>
{
    let min = 0;

    for(let i in arr)
    {
        if(arr[i] < min)
        {
            min = arr[i];
        }
    }
    
    if(typeof min === 'number')
        return (min);
    else
        return("error");
}
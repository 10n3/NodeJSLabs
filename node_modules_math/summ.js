module.exports = (arr) => 
{
    let summ = 0;

    for (let i in arr)
    {
        summ += arr[i];
    }

    if(typeof summ === 'number')
        return (summ);
    else
        return("error");
}